services:
  - type: web
    name: cryptoclash-server
    env: node
    buildCommand: npm install
    startCommand: node server-production.js
    envVars:
      - key: NODE_ENV
        value: production
